/**
 * Production server entry point
 * This file runs the full backend API for Autoscale deployment
 */

// Import and run the full backend (same as development but with NODE_ENV=production)
import "./dist/index.js";
