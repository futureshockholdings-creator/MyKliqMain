
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd client
        - npm install --force
    build:
      commands:
        - cd client
        - rm -rf dist
        - npm run build
  artifacts:
    baseDirectory: client/dist
    files:
      - '**/*'
  cache:
    paths:
      - client/node_modules/**/*

